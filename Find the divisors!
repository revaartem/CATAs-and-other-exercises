def divisors(integer):
    numbers = []
    for i in range(2, integer):
        if integer % i == 0:
            numbers.append(i)
    if numbers == []:
        return f'{integer} is prime'
    return numbers
